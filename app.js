// Legacy note:
// The former monolithic app.js has been split into domain-based scripts under app/.
// The actual runtime entry is now declared in index.html.
console.warn("Legacy app.js is no longer the active entry point. Load app/core, app/workspace, app/render, app/animation, app/io, and app/ui via index.html.");
